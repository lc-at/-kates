apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lcat-dev-wildcard
  namespace: kube-system
spec:
  secretName: lcat-dev-wildcard-tls
  issuerRef:
    name: letsencrypt-prod-acmedns
    kind: ClusterIssuer
  dnsNames:
    - "*.lcat.dev"
