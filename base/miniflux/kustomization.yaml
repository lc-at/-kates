apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: miniflux-stack

resources:
  - namespace.yaml
  - secret.yaml
  - db.yaml
  - app.yaml
  - ingress.yaml
